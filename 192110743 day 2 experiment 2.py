def area(n,len):
      a=0
      for i in range(len):
        for j in range(i+1,len):
              a=max(a,min(n[j],n[i]*(j-i))
        return a
i=int(input("enter the number of elements"))
ist=[]
for i in range(0,i);
      n=int(input("enter the elements:"))
      ist.append
if(i<=0)or(n<=0)):
    print("invald")
else:
      len1=len
      print(area(n,len1))
